{% extends 'base.html.twig' %}

{%  block title %}Roles{% endblock %}

{%  block body %}
    <div class="container-fluid user-admin">
        <div class="row">
            <div class="col-lg-2">
                {% include('nav-left.html.twig') %}
            </div>

            <div class="col-lg-10">

                <h4>Modifier le rôle de l'utilisateur</h4>
                {{ form(userForm) }}

            </div>
        </div>
    </div>


{% endblock %}